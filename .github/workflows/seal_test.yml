name: Seal Audit (No Token)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  seal-scan:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Download and run Seal CLI
      run: |
        curl -sSfL https://github.com/seal-community/cli/releases/download/latest/seal-linux-amd64-latest -o seal
        chmod +x seal
        ls -la                          # 🧪 Sanity check: confirm requirements.txt is there
        ./seal scan --fs python:requirements.txt
